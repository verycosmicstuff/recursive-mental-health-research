# Python
__pycache__/
*.py[cod]
*$py.class
venv/
env/

# Runtime / log files — do not commit
app.log
app.log.old
PAUSED.txt
*.backup

# Experiment data — generated at runtime, stays local
experiments/
results.tsv
results_macro.tsv

# Examples folder is intentionally tracked
!examples/
