# Python
__pycache__/
*.py[cod]
*$py.class
venv/
env/

# Runtime / log files — do not commit
app.log
app.log.old
PAUSED.txt
*.backup

# Experiment data — generated at runtime, but tracked per user request
# app.log and backups are still ignored.

# Examples folder is intentionally tracked
!examples/
